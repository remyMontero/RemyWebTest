#toplevel
pipeline{
    #where to execute
    agent any
    #where all of the work happens
    stages{
        stage ("build"){
            steps{
                echo 'Jenkins File building the application'
            }
        }
        stage ("test"){
            steps{
                echo 'Jenkins File testing the application'
            }
        }
        stage ("deploy"){
            steps{
                echo 'Jenkins File deploying the application'
            }
        }
    }
}